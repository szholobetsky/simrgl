# Minimal requirements - only for basic Word2Vec functionality
# Use this if you want to start with just the 'own' vectorizer model

# Core dependencies
numpy>=1.21.0
scipy>=1.7.0
scikit-learn>=1.0.0
tqdm>=4.62.0
gensim>=4.1.0

# For analysis and visualization
pandas>=1.3.0
matplotlib>=3.4.0
seaborn>=0.11.0

# For downloading files (GloVe)
requests>=2.25.0

# This minimal setup supports:
# - VECTORISER_MODEL = 'own' (Word2Vec)
# - Basic analysis and visualization
# - Experiment framework
#
# To add other models later, install additional packages:
# For FastText: pip install fasttext-wheel (Windows) or fasttext (Linux)
# For BERT: pip install torch transformers
# For LLM: pip install sentence-transformers
# For OpenAI: pip install openai